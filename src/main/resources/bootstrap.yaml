spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8841
        prefix: nine-nacos-config-server
        file-extension: yaml
        cluster-name: nine
        namespace: dev

      discovery:
        cluster-name: nine
        server-addr: 127.0.0.1:8841
        namespace: dev
